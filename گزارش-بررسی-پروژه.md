# گزارش بررسی کامل پروژه — مینی‌اپ «Lovely»

**تاریخ:** ۱۴۰۵/۰۶/۰۸ (۳۰ اوت ۲۰۲۶)
**دامنه:** بررسی کامل کد، رفع باگ‌های بحرانی، و افزودن مجموعه‌ای گسترده از بخش‌های جدید به‌صورت فول‌استک
**نوع پروژه:** اپلیکیشن دوستیابی به‌شکل Telegram Mini App

---

## ۱. خلاصه اجرایی

پروژه یک مینی‌اپ دوستیابی درون تلگرام است که از سه بخش اصلی ساخته شده: یک ربات grammY، یک سرور Express که هم API و هم فایل‌های استاتیک فرانت‌اند را سرو می‌کند، و یک رابط کاربری React 19 (Vite) با طراحی تیره و «گلَس‌مورفیسم» و متن کامل فارسی.

بررسی روی سه محور انجام شد: نخست شناسایی و رفع باگ‌های بحرانی (به‌ویژه یک شکاف امنیتی جدی در احراز هویت)، سپس افزودن چهار «بستهٔ قابلیت» کامل شامل ۹ مدل دیتابیس جدید، ۳۳ اندپوینت تازه و ۱۵ صفحهٔ رابط کاربری، و در پایان اعتبارسنجی ساختاری کل درخت کد.

مهم‌ترین یافته این بود که پیش از این هیچ اعتبارسنجی‌ای روی هویت تلگرامِ ارسال‌شده از کلاینت انجام نمی‌شد؛ یعنی هر کسی می‌توانست با ارسال شناسهٔ یک کاربر دیگر، به‌جای او عمل کند. این مورد به‌همراه چند باگ ساختاری دیگر رفع شد. تمام قابلیت‌های جدید با همان زبان طراحی موجود (فونت Vazirmatn، گرادیان صورتی #FF2A7A تا بنفش #8C30F5، سطوح شیشه‌ای) پیاده‌سازی شده‌اند تا از نظر بصری یکدست بمانند.

وضعیت نهایی: تمام بررسی‌های ساختاری با موفقیت عبور کرده‌اند. تنها گام باقی‌مانده اجرای «مهاجرت دیتابیس» و «ساخت فرانت‌اند» روی ویندوز است که در بخش ۸ توضیح داده شده (این گام‌ها فقط روی ماشین شما قابل اجراست، نه در محیط بررسی).

---

## ۲. معماری پروژه

ساختار پروژه پس از تغییرات به این شکل است:

```
my_app/
├── server.js              سرور اصلی Express + اندپوینت‌های هستهٔ اپ
├── bot.js                 ربات تلگرام (grammY)
├── lib/                   ← پوشهٔ جدید: منطق مشترک بک‌اند
│   ├── prisma.js          نمونهٔ واحد Prisma Client
│   ├── telegramAuth.js    ← اعتبارسنجی امضای initData تلگرام (لایهٔ امنیتی)
│   ├── notify.js          هلپر مرکزی اعلان (رکورد درون‌برنامه‌ای + پیام تلگرام)
│   └── helpers.js         توابع کمکی مشترک
├── routes/                ← پوشهٔ جدید: اندپوینت‌ها به تفکیک بسته
│   ├── monetization.js    بستهٔ A — درآمد و تعامل
│   ├── profile.js         بستهٔ B — پروفایل و کشف
│   ├── settings.js        بستهٔ C — تنظیمات، ایمنی و فیلترها
│   └── social.js          بستهٔ D — اجتماعی و رویدادها
├── prisma/
│   ├── schema.prisma      ۱۳ مدل (۹ مدل جدید افزوده شد)
│   └── seed.js            داده‌های نمونه برای همهٔ مدل‌های جدید
└── frontend/
    └── src/
        ├── api.js         ← لایهٔ واحد فراخوانی API
        ├── sections.css   ← استایل تمام بخش‌های جدید (~۱۰۳۶ خط)
        ├── components/
        │   ├── UI.jsx     ← کامپوننت‌های پایه: PageHeader، Loading، EmptyState، Avatar، Segmented، Toggle، useToast
        │   └── Stories.jsx← نوار استوری‌ها
        └── pages/         ۱۵ صفحهٔ جدید (فهرست در بخش ۵)
```

نکتهٔ کلیدی معماری: اندپوینت‌های هر بستهٔ قابلیت در یک ماژول روتر جداگانه قرار گرفته و همگی زیر پیشوند `/api` سوار (mount) شده‌اند. این کار server.js را تمیز نگه می‌دارد و افزودن بخش‌های آینده را ساده می‌کند.

---

## ۳. باگ‌های بحرانی که رفع شد

### ۳-۱. شکاف امنیتی در احراز هویت (بحرانی)

**مشکل:** فرانت‌اند شناسهٔ تلگرام کاربر را در بدنه یا پارامتر درخواست می‌فرستاد و سرور بدون هیچ راستی‌آزمایی به آن اعتماد می‌کرد. در نتیجه هر کسی می‌توانست با جعل شناسهٔ فرد دیگری، پروفایل او را تغییر دهد، به‌جای او پیام بفرستد، لایک کند یا خرید ثبت کند.

**راه‌حل:** یک لایهٔ اعتبارسنجی مطابق مشخصات رسمی تلگرام اضافه شد (`lib/telegramAuth.js`). این ماژول امضای HMAC-SHA256 رشتهٔ `initData` را با استفاده از توکن ربات بررسی می‌کند و مقایسه به‌صورت زمان‌ثابت (`timingSafeEqual`) انجام می‌شود تا در برابر حملهٔ زمان‌سنجی مقاوم باشد. یک بررسی تازگی ۲۴ ساعته نیز برای جلوگیری از حملهٔ بازپخش (replay) گنجانده شده است.

سازوکار در دو بخش کار می‌کند:

- میان‌افزار سراسری `attachTelegramUser` که اگر هدر معتبر `x-telegram-init-data` وجود داشته باشد، کاربر تأییدشده را به `req.tg` می‌چسباند.
- محافظ `authorizeTelegramId` که روی هر اندپوینت حساس بررسی می‌کند شناسه‌ای که درخواست روی آن عمل می‌کند با هویت تأییدشده یکی باشد؛ در غیر این صورت پاسخ ۴۰۳ برمی‌گرداند.

این محافظ روی همهٔ نقاط حساس نصب شده است: به‌روزرسانی پروفایل، ثبت لایک/اکشن، ارسال پیام، حذف/آنمَچ، ارسال هدیه و تمام اندپوینت‌های نوشتنیِ چهار بستهٔ جدید. برای اینکه توسعهٔ محلی مختل نشود، در حالت توسعه محافظ عبور می‌دهد؛ اما با تنظیم متغیر محیطی `REQUIRE_TELEGRAM_AUTH=1` در پروداکشن، ارسال initData معتبر اجباری می‌شود.

### ۳-۲. ترتیب نادرست مسیر catch-all در Express (بحرانی)

**مشکل:** در Express اگر مسیر عمومی `app.get("*")` (که فایل اصلی فرانت‌اند را برمی‌گرداند) پیش از تعریف اندپوینت‌های API قرار گیرد، تمام درخواست‌های API را می‌بلعد و آن‌ها هرگز اجرا نمی‌شوند.

**راه‌حل:** ترتیب چیدمان اصلاح شد. اکنون ماژول‌های روتر (خطوط ۶۶۰ تا ۶۶۳) و همهٔ اندپوینت‌های هسته پیش از catch-all قرار دارند و `app.get("*")` در خط ۶۶۶، یعنی درست پیش از `app.listen`، آخرین مسیر است.

### ۳-۳. نبود کلید یکتای مرکب و شرایط رقابتی (پرخطر)

**مشکل:** عملیات‌هایی مثل لایک‌کردن، دیدن استوری یا پیوستن به رویداد در صورت ارسال هم‌زمان یا تکراری، رکوردهای دوگانه ایجاد می‌کردند.

**راه‌حل:** کلیدهای یکتای مرکب روی مدل‌های مربوطه تعریف شد — `fromUserId_toUserId` روی Like، `storyId_viewerId` روی StoryView و `eventId_userId` روی EventAttendee — و عملیات‌ها به‌جای `create` از `upsert` یا `$transaction` استفاده می‌کنند تا ثبت‌ها ایدمپوتنت (تکرارپذیرِ بی‌خطر) باشند. برای نمونه، منطق مَچ‌شدن اکنون درون تراکنش، لایک متقابل را بررسی و مَچ را به‌صورت اتمیک ایجاد می‌کند.

### ۳-۴. اندپوینت بدون پشتوانه (متوسط)

**مشکل:** فرانت‌اند تابع `api.report` را فراخوانی می‌کرد اما اندپوینت `POST /api/report` در بک‌اند وجود نداشت؛ گزارش تخلف کاربران در عمل کار نمی‌کرد.

**راه‌حل:** اندپوینت `POST /report` در `routes/settings.js` اضافه شد که وجود کاربر مبدأ و مقصد و معتبربودن دلیل گزارش را بررسی می‌کند، از گزارش‌دادن به خود جلوگیری می‌کند و رکورد Report را با وضعیت `open` ثبت می‌نماید.

---

## ۴. تغییرات دیتابیس (Prisma)

اسکیمای دیتابیس از ۴ مدل به ۱۳ مدل گسترش یافت. ۹ مدل جدید:

| مدل | نقش |
|---|---|
| `ProfilePrompt` | پرامپت‌های پروفایل (پرسش/پاسخ‌های شخصیت‌محور) |
| `Story` | استوری‌های موقت کاربران |
| `StoryView` | ثبت بازدید استوری (کلید مرکب `storyId_viewerId`) |
| `Gift` | هدایای مجازی بین کاربران |
| `Event` | رویدادهای واقعی/آنلاین |
| `EventAttendee` | شرکت‌کنندگان رویداد (کلید مرکب `eventId_userId`) |
| `Purchase` | تاریخچهٔ خرید سکه/اشتراک |
| `Report` | گزارش تخلف کاربران |
| `Notification` | اعلان‌های درون‌برنامه‌ای |

همچنین به مدل `User` فیلدهای تازه افزوده شد: پرچم‌های اعلان (`notifyMatches`، `notifyLikes`، `notifyMessages`)، حالت ناشناس (`incognito`)، شهر پاسپورت (`passportCity`)، و ترجیحات کشف (`prefAgeMin`، `prefAgeMax`، `maxDistance`). فایل `seed.js` نیز برای پرکردن همهٔ این مدل‌ها با دادهٔ نمونه به‌روزرسانی شد.

---

## ۵. بخش‌های جدید (فول‌استک)

چهار بستهٔ قابلیت به‌صورت کامل — فرانت‌اند + اندپوینت Express + مدل Prisma — ساخته شد. مجموعاً ۱۵ صفحهٔ جدید افزوده شد که همگی از کامپوننت‌های مشترک `UI.jsx` و لایهٔ واحد `api.js` استفاده می‌کنند.

### بستهٔ A — درآمد و تعامل

اشتراک **Lovely Premium** (صفحهٔ `Premium.jsx` با پلن‌ها و مزایا)، **فروشگاه سکه** (`Store.jsx`)، صفحهٔ **چه کسانی تو را لایک کرده‌اند** (`LikesYou.jsx`)، و **انتخاب‌های برتر روزانه** (`TopPicks.jsx`). این‌ها از اندپوینت‌های `premium/plans`، `premium/status`، `premium/subscribe`، `store`، `store/purchase`، `likes-you` و `top-picks` پشتیبانی می‌گیرند.

### بستهٔ B — پروفایل و کشف

**پرامپت‌های پروفایل** (`Prompts.jsx` — انتخاب از کاتالوگ و پاسخ‌دهی، که در صفحهٔ پروفایل هم نمایش داده می‌شود)، **تأیید هویت / نشان آبی** (`Verification.jsx`)، **هدایای دریافتی** (`Gifts.jsx`)، و نوار **استوری‌ها** (`Stories.jsx`). اندپوینت‌ها: `stories`، `stories/view`، `prompts/catalog`، `prompts`، `verification` و `gifts`.

### بستهٔ C — تنظیمات، ایمنی و فیلترها

**فیلترهای جستجو** (`Filters.jsx` — بازهٔ سنی، فاصله، ترجیح جنسیت)، **تنظیمات اعلان و حریم خصوصی** (`Settings.jsx` — با به‌روزرسانی خوش‌بینانه و بازگشت خودکار در خطا)، و **مرکز ایمنی** (`SafetyCenter.jsx` — نکات ایمنی، توضیح گزارش/مسدودسازی، و اطلاعات تماس اضطراری). اندپوینت‌ها: `preferences`، `settings`، `report/reasons`، `report` و `block`.

### بستهٔ D — اجتماعی و رویدادها

**رویدادها** (`Events.jsx` — دسته‌بندی، پیوستن/خروج درجا، مودال جزئیات با فهرست شرکت‌کنندگان)، **جدول برترین‌ها** (`Leaderboard.jsx` — سکوی سه‌نفرهٔ کلاسیک ۲-۱-۳)، **پاسپورت** (`Passport.jsx` — تغییر موقعیت مکانی برای دیدن افراد شهرهای دیگر)، و **اعلان‌ها** (`Notifications.jsx` — با علامت‌گذاری همه به‌عنوان خوانده‌شده). اندپوینت‌ها: `events`، `events/:id`، `events/:id/join`، `events/:id/leave`، `leaderboard`، `passport`، `passport/cities` و `notifications`.

### ناوبری

نوار پایین به ۵ تب اصلی (کشف، گردش، پیام‌ها، پروفایل، بیشتر) به‌روزرسانی شد و یک صفحهٔ هاب **«بیشتر»** (`More.jsx`) افزوده شد که به‌شکل شبکه‌ای دسترسی به همهٔ بخش‌های ثانویه را فراهم می‌کند. تب پروفایل نیز بازطراحی شد تا لینک‌های تنظیمات به‌شکل گروه‌بندی‌شده (پروفایل، کشف، تنظیمات، سایر) نمایش داده شوند.

---

## ۶. فهرست اندپوینت‌های جدید API

مجموعاً ۳۳ اندپوینت تازه در چهار ماژول روتر:

**`routes/monetization.js`** — `GET /premium/plans`، `GET /premium/status/:telegramId`، `POST /premium/subscribe`، `GET /store/:telegramId`، `POST /store/purchase`، `GET /likes-you/:telegramId`، `GET /top-picks/:telegramId`

**`routes/profile.js`** — `GET /stories/:telegramId`، `POST /stories`، `POST /stories/:storyId/view`، `GET /prompts/catalog`، `GET /prompts/:telegramId`، `PUT /prompts/:telegramId`، `GET /verification/:telegramId`، `POST /verification/:telegramId`، `GET /gifts/:telegramId`، `POST /gifts`

**`routes/settings.js`** — `GET /preferences/:telegramId`، `PUT /preferences/:telegramId`، `GET /settings/:telegramId`، `PUT /settings/:telegramId`، `GET /report/reasons`، `POST /report`، `POST /block`

**`routes/social.js`** — `GET /events`، `GET /events/:id`، `POST /events/:id/join`، `POST /events/:id/leave`، `GET /leaderboard`، `PUT /passport/:telegramId`، `GET /passport/cities`، `GET /notifications/:telegramId`، `POST /notifications/:telegramId/read`

---

## ۷. کیفیت، امنیت و اعتبارسنجی

از آنجا که زنجیرهٔ ابزار ساخت (Vite/Rolldown و Prisma CLI) در محیط بررسی به‌دلیل باینری‌های مخصوص ویندوز و دسترسی‌نداشتن به رجیستری npm قابل اجرا نبود، یک اعتبارسنج ساختاری مستقل نوشته شد تا سلامت کل درخت کد تضمین شود. نتایج:

بررسی توازن براکت‌ها، پرانتزها و آکولادها در تمام فایل‌های JS/JSX/CSS بدون خطا عبور کرد. تمام ایمپورت‌های نسبی به فایل‌های واقعی رفع (resolve) شدند و هر ایمپورت نام‌دار یا پیش‌فرض به یک export واقعی در فایل مقصد اشاره می‌کند. هر ۵۳ آیکون lucide-react استفاده‌شده به فایل آیکون واقعی نگاشت شدند. در سمت بک‌اند، دستور `node --check` روی همهٔ ۹ فایل بک‌اند و `prisma/seed.js` بدون خطای نحوی عبور کرد و تأیید شد که همهٔ ارجاع‌های `prisma.<model>` با نام مدل‌های اسکیما منطبق‌اند.

از منظر امنیتی، فایل `.env` (شامل `BOT_TOKEN`، `DATABASE_URL`، `PORT`، `FRONTEND_URL`) و پایگاه‌دادهٔ `dev.db` در `.gitignore` قرار دارند و مقادیر محرمانه در هیچ کجای کد یا این گزارش افشا نشده‌اند.

---

## ۸. کارهایی که باید روی ویندوز انجام دهید

سه مورد زیر باید روی ماشین خودتان (که باینری‌های درست ویندوز و دسترسی به npm را دارد) اجرا شوند؛ این‌ها در محیط بررسی قابل اجرا نبودند:

نخست، تولید کلاینت Prisma و اعمال مهاجرت برای ۹ مدل و فیلدهای جدید:

```
npx prisma migrate dev --name add_engagement_features
npx prisma generate
npx prisma db seed
```

دوم، ساخت فرانت‌اند:

```
cd frontend
npm run build
```

سوم، برای فعال‌کردن اجبار احراز هویت در پروداکشن، متغیر محیطی زیر را در `.env` تنظیم کنید (در توسعهٔ محلی لازم نیست):

```
REQUIRE_TELEGRAM_AUTH=1
```

پس از این سه گام، اپ کاملاً آمادهٔ اجراست.

---

## ۹. پیشنهادهای گام بعد

چند موردی که ارزش رسیدگی در آینده را دارند: افزودن صفحه‌بندی (pagination) به فهرست‌های بلند مانند اعلان‌ها و جدول برترین‌ها برای مقیاس‌پذیری؛ اتصال درگاه پرداخت واقعی (مثلاً Telegram Payments یا Stars) به اندپوینت‌های `store/purchase` و `premium/subscribe` که در حال حاضر خرید را شبیه‌سازی می‌کنند؛ افزودن آزمون‌های خودکار (integration test) برای مسیرهای حساس احراز هویت؛ و در نظر گرفتن نرخ‌محدودسازی (rate limiting) روی اندپوینت‌های نوشتنی برای مقابله با سوءاستفاده. هیچ‌کدام از این‌ها مانع اجرای فعلی اپ نیستند و صرفاً بهبودهای بلندمدت‌اند.

---

*این گزارش خلاصه‌ای از بررسی کامل کد، رفع باگ‌های بحرانی و افزودن چهار بستهٔ قابلیت فول‌استک است. تمام تغییرات با زبان طراحی موجود اپ هماهنگ‌اند و اعتبارسنجی ساختاری کامل روی آن‌ها انجام شده است.*
